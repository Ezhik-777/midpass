# Ваш емейл, под которым вы зарегистрированы на сайте q.midpass.ru
EMAIL=
# Ваш пароль
PASSWORD=

# Если вы в Грузии, то ничего ниже трогать не нужно
# Таймзона
TIMEZONE=Asia/Tbilisi
# Грузия
COUNTRY_ID=88655495-3b8c-f56d-5337-0f2743a7bfed
# Тбилиси - Консульская служба Секции интересов РФ
SERVICE_PROVIDER_ID=b8af6319-9d8d-5bd9-f896-edb8b97362d0

# Опционально: API-ключ rucaptcha.com / 2captcha.com для решения CAPTCHA.
# Если не задан — используется встроенный распознаватель.
# После редизайна сайта (Angular SPA, 2026) встроенный распознаватель
# капчу не разгадывает, поэтому ключ rucaptcha де-факто обязателен.
# RUCAPTCHA_API_KEY=
# RUCAPTCHA_ENDPOINT=https://api.rucaptcha.com

# Опционально: оптимизация локального pre-check (Step 0 в confirm-queue).
# Если RENEWAL_INTERVAL_HOURS задан — pre-check разрешает попытку только
# когда с момента последнего успешного подтверждения прошло не меньше
# указанного числа часов. Это экономит запросы к серверу и капчи между
# окнами подтверждения. Если параметр пуст или не задан — pre-check
# всегда разрешает попытку (поведение по умолчанию).
# Реальный интервал на новом API q.midpass.ru — около 24 часов, поэтому
# безопасные значения 20-23. Меньше — лишний расход капчи, больше — риск
# пропустить окно из-за дрейфа cron.
# RENEWAL_INTERVAL_HOURS=20
#
# После ответа сервера canConfirm:false бот фиксирует момент и в течение
# этого окна (минут) не делает повторных попыток. Default 60.
# NEGATIVE_PROBE_COOLDOWN_MINUTES=60
#
# Sanity-страховка: если pre-check блокирует попытки дольше этого числа
# часов, всё равно сделать одну попытку. Default 26.
# MAX_SKIP_AGE_HOURS=26
